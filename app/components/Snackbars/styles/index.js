'use strict';

import { mixins, variables } from '../../../styles/js';

export const styles = theme => ({
  margin: {
    margin: theme.spacing(1)
  },
  root: {
    top: 10,
    right: 15,
    left: `unset`
  }
});
